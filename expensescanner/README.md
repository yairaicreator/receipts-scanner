# -

אפליקצייה לסריקה של חשבונית

The working app lives at the root of this repo (`src/`, `api/`). See
**[SETUP.md](SETUP.md)** to run or deploy it.

`אפליקציה לריקי/` is the original Claude Design prototype the app was built
from, and `chats/` is the design conversation behind it.
